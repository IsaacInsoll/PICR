{
  "name": "picr",
  "version": "1.6.0",
  "description": "",
  "type": "module",
  "directories": {
    "doc": "docs",
    "test": "tests"
  },
  "scripts": {
    "test": "npm run test:api && npm run test:e2e",
    "test:api": "vitest run",
    "test:unit": "vitest run --config vitest.unit.config.mts",
    "test:e2e": "playwright test --config tests/e2e/playwright.config.ts",
    "test:e2e:fresh": "npm run build:local && npm run test:e2e",
    "test:e2e:install": "playwright install chromium",
    "test:e2e:install:ci": "playwright install --with-deps chromium",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "check": "npm run format:check && npm run lint && npm run tsc && npm run i18n:check",
    "i18n:check": "i18next-cli status && i18next-cli status --unused && npm run i18n:check:contracts",
    "i18n:check:contracts": "vitest run --config vitest.unit.config.mts tests/api/i18nCatalogContracts.unit.test.ts",
    "lint": "npm run lint:shared && npm run lint:backend && npm run lint:frontend && npm run lint:app && npm run lint:ping",
    "lint:shared": "npm --prefix shared run lint",
    "lint:backend": "npm --prefix backend run lint",
    "lint:frontend": "npm --prefix frontend run lint",
    "lint:app": "npm --prefix app run lint",
    "lint:ping": "npm --prefix ping run lint",
    "tsc": "npm run tsc:shared && npm run tsc:backend && npm run tsc:frontend && npm run tsc:app && npm run tsc:ping",
    "tsc:shared": "cd shared && npx tsc --noEmit",
    "tsc:backend": "cd backend && npx tsc --noEmit",
    "tsc:frontend": "cd frontend && npx tsc --noEmit",
    "tsc:app": "cd app && npm run typecheck",
    "tsc:ping": "npm --prefix ping run typecheck",
    "workflow": "act push --pull=false --artifact-server-path $PWD/.artifacts -P ubuntu-24.04=catthehacker/ubuntu:act-latest",
    "install-all": "npm ci && npm --prefix shared ci && npm --prefix backend ci && npm --prefix frontend ci && npm --prefix app ci && npm --prefix ping ci && sh ./copy-backend-files.sh && npm --prefix dist ci --omit=dev",
    "build:local": "npm --prefix backend run build && npm run build:lightroom && npm --prefix frontend run build && npm --prefix dist ci --omit=dev",
    "gql": "npm --prefix backend run gql:generate && prettier --write shared/urql/graphql.schema.json frontend/src/fonts.generated.ts app/src/fonts.generated.ts",
    "release": "dotenv -- release-it --only-version",
    "release:ping": "node ping/scripts/release.mjs",
    "release:app:auth": "cd app && npx eas whoami >/dev/null",
    "release:app:install": "npm --prefix shared ci && npm --prefix app ci --ignore-scripts",
    "release:app:lint": "npm --prefix shared run lint && npm --prefix app run lint",
    "release:app:tsc": "npm run tsc:shared && npm run tsc:app",
    "release:app:doctor": "cd app && (npx expo-doctor || echo '[release:app:doctor] expo-doctor reported issues (advisory, non-blocking)')",
    "release:app:export:ios": "bash scripts/release-app-export-ios.sh",
    "release:app:export:android": "cd app && npx expo export --platform android",
    "release:app:preflight": "npm run release:app:auth && npm run release:app:install && npm run release:app:lint && npm run release:app:tsc && npm run release:app:doctor && npm run release:app:export:ios",
    "release:app:dry": "npm run release:app:preflight",
    "release:app:version": "npm --prefix app run release:version",
    "release:app:eas:ios": "cd app && npx eas build --platform ios --profile production --auto-submit --non-interactive --wait",
    "release:app:eas:android": "cd app && npx eas build --platform android --profile production --auto-submit --non-interactive --wait",
    "release:app:eas": "npm run release:app:eas:ios && npm run release:app:eas:android",
    "release:app:ios": "npm run release:app:preflight && npm run release:app:version && npm run release:app:eas:ios",
    "release:app:android": "npm run release:app:preflight && npm run release:app:version && npm run release:app:eas:android",
    "release:app": "npm run release:app:preflight && npm run release:app:version && npm run release:app:eas",
    "build:lightroom": "cd lightroom/picr.lrplugin && zip -rq ../../frontend/public/picr-lightroom-plugin.zip .",
    "start:server-ts": "cd backend && npx tsc --noEmit -w --preserveWatchOutput",
    "start:server": "TSX_TSCONFIG_PATH=backend/tsconfig.json backend/node_modules/.bin/tsx watch backend/app.ts",
    "start:client": "cd frontend && npm start",
    "start:css:types": "cd frontend && npm run css:types:watch",
    "start:db": "docker compose up -d",
    "start": "concurrently -c \"gray,blue,magenta,cyan,yellow\" npm:start:*"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "concurrently": "^10.0.5",
    "dotenv-cli": "^11",
    "eslint-plugin-import": "^2.32.0",
    "eslint-plugin-unused-imports": "^4.4.1",
    "i18next-cli": "^1.69.2",
    "prettier": "^3.9",
    "release-it": "^21.0.2",
    "typescript": "^6.0.3",
    "urql": "^5.0.4",
    "vitest": "^4.1.11"
  },
  "overrides": {
    "lodash": "^4.18.1"
  },
  "release-it": {
    "npm": {
      "publish": false
    },
    "git": {
      "requireBranch": "master",
      "commitMessage": "🚀 Release ${version}",
      "commit": true,
      "tag": true,
      "push": true
    },
    "github": {
      "release": true
    },
    "hooks": {
      "before:github:release": "node scripts/verify-release.mjs ${version} ${tagName}",
      "before:bump": "node lightroom/scripts/update-info-lua.js ${version}"
    }
  },
  "author": "Isaac Insoll",
  "license": "ISC",
  "engines": {
    "node": "24.13.0"
  }
}
