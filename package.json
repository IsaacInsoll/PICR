{
  "name": "picr",
  "version": "0.9.5",
  "description": "",
  "type": "module",
  "directories": {
    "doc": "docs",
    "test": "tests"
  },
  "scripts": {
    "test": "vitest run",
    "workflow": "act push --pull=false --artifact-server-path $PWD/.artifacts -P ubuntu-24.04=catthehacker/ubuntu:act-latest",
    "gql": "graphql-codegen --config codegen.ts",
    "release": "dotenv -- release-it --only-version",
    "build:lightroom": "cd lightroom/picr.lrplugin && zip -r ../../frontend/public/picr-lightroom-plugin.zip .",
    "start:server-ts": "cd backend && tsc -w",
    "start:server": "nodemon --delay 0.5 --watch .env --watch dist/server dist/server/backend/app.js",
    "start:client": "cd frontend && npm start",
    "start:db": "docker compose up -d",
    "start": "concurrently -c \"gray,blue,magenta,cyan\" npm:start:*"
  },
  "devDependencies": {
    "@graphql-codegen/cli": "^5.0.5",
    "@graphql-codegen/client-preset": "^4.8.0",
    "@graphql-codegen/introspection": "^4.0.3",
    "@release-it/bumper": "^7.0.2",
    "concurrently": "^9.2.1",
    "decompress": "^4.2.1",
    "docker-compose": "^1.3",
    "dotenv-cli": "^11",
    "nodemon": "^3.1.11",
    "prettier": "^3.8",
    "release-it": "^19.2.4",
    "typescript": "^5.9.3",
    "vitest": "^3.1.1"
  },
  "release-it": {
    "npm": {
      "publish": false
    },
    "git": {
      "requireBranch": "master",
      "commitMessage": "🚀 Release ${version}",
      "commit": true,
      "tag": true,
      "push": true
    },
    "github": {
      "release": true
    },
    "plugins": {
      "@release-it/bumper": {
        "out": {
          "file": "./version.txt",
          "type": "text/plain",
          "consumeWholeFile": true
        }
      }
    },
    "hooks": {
      "before:bump": "node lightroom/scripts/update-info-lua.js ${version}"
    }
  },
  "author": "Isaac Insoll",
  "license": "ISC",
  "engines": {
    "node": "24.13.0"
  }
}
